# Backend

Current Django backend lives in repository root:

- `khatapro/` project settings and ASGI/WSGI
- domain apps: `accounts/`, `commerce/`, `orders/`, `pos/`, etc.
- mode switch app: `system_mode/`

This `backend/` directory is provided as architecture marker for monorepo docs.
